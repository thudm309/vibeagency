# Pass the slug as a query parameter so JavaScript can detect it
/blog/:slug.html /blog-post.html?slug=:slug 200

# Optional: Redirect /blog to homepage blog section
/blog /index.html#blog 302

# Optional: Handle trailing slashes
/blog/ /index.html#blog 302